[project]
name = "compliance-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shian07", email = "shian07@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
compliance-app = "compliance_app:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
